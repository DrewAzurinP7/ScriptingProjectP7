using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LinearInterpolation : MonoBehaviour
{/*
    //In this case,, result = 4
    float result = Mathf.Lerp(3f, 5f, 0.5f);
    //Mathf.Lerp function takes 3 float parameters: One represents the value to interpolate from; another represents the value to interpolate to & a final float represting how far to interpolate.

    Vector3 from = new Vector3 (1f, 2f, 3f);
    Vector3 to = new Vector3 (5f, 6f, 7f);

    //Here result = (4, 5, 6)
    Vector3 results = Vector3.Lerp(from, to, 0.75f);
    //The 4 represent 75% between 1 & 5 &, & 5 represents 75% of the way between 2 & 6

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        Light.intensity = Mathf.Lerp(Light.intensity, 8f, 0.5f);
        //Every frame that runs will be set to 0, then to 4, then to 6, then to 7, then to 7.5 & so on

        //However, if we add Time.deltaTime, the frame will happen per second instead of per frame
        light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f * Time.deltaTime);
    }
    */
}
